package com.feastorder.model;

import java.sql.Timestamp;
import java.util.List;

/**
 * MODEL: Order
 * ------------------------------------------------------------
 * Represents a row in your `orders` table, PLUS a list of items in that order
 * (which normally live in a separate `order_items` join table in the DB).
 *
 * TODO for your team:
 * 1. Add fields:
 *      - int orderId
 *      - int userId              (which customer placed it)
 *      - double totalPrice
 *      - String status            (e.g. "Pending", "Confirmed")
 *      - Timestamp orderTime
 *      - List<OrderItem> items    (see OrderItem.java stub below)
 *
 * 2. Generate getters/setters.
 *
 * Maps to rubric "2c Dynamic Order Processing" and Admin Dashboard's
 * "view customer orders" feature (rubric 2b).
 */
public class Order {

    // TODO: declare fields here

    // TODO: generate getters and setters here

}

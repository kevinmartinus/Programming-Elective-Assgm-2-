package com.feastorder.model;

/**
 * MODEL: User
 * ------------------------------------------------------------
 * Represents a row in your `users` table (register/login system).
 *
 * TODO for your team:
 * 1. Add private fields matching your DB schema, e.g.:
 *      - int userId
 *      - String username
 *      - String email
 *      - String password        (store HASHED password, never plain text)
 *      - String phoneNumber
 *      - String role             ("customer" or "admin")
 *      - Timestamp createdAt
 *
 * 2. Generate standard getters/setters for each field
 *    (Eclipse: right-click -> Source -> Generate Getters and Setters)
 *
 * 3. Optionally add a no-args constructor and an all-args constructor.
 *
 * This class should contain NO database logic and NO business logic —
 * it is a plain data holder (a "JavaBean"), which also satisfies your
 * "at least 3 Java EE technologies" requirement (JavaBeans).
 */
public class User {

    // TODO: declare fields here

    // TODO: generate getters and setters here

}

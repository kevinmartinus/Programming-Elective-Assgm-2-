package com.feastorder.dao;

import com.feastorder.model.MenuItem;
import com.feastorder.model.Category;

/**
 * DAO: MenuDAO
 * ------------------------------------------------------------
 * All SQL related to `menu_items` and `categories` tables.
 *
 * TODO for your team, implement:
 *
 * 1. List<MenuItem> getAllMenuItems()
 *    - SELECT * FROM menu_items
 *
 * 2. List<MenuItem> getMenuItemsByCategory(int categoryId)
 *    - SELECT * FROM menu_items WHERE category_id = ?
 *
 * 3. MenuItem getMenuItemById(int itemId)
 *    - For the item detail view (ingredients, price, ratings)
 *
 * 4. List<Category> getAllCategories()
 *
 * 5. boolean addMenuItem(MenuItem item)      -- admin only
 * 6. boolean updateMenuItem(MenuItem item)   -- admin only
 * 7. boolean deleteMenuItem(int itemId)      -- admin only
 *
 * 8. boolean addCategory(Category category)      -- admin only
 * 9. boolean deleteCategory(int categoryId)       -- admin only
 *
 * Maps to rubric "1a Page Development" (Menu Page) and
 * "2b Admin System & DB Connectivity" (CRUD on menu/categories).
 */
public class MenuDAO {

    // TODO: implement methods described above

}

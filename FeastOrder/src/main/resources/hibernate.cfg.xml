<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "https://hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>

        <!-- matches db.properties -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/feastorder_db?useSSL=false&amp;serverTimezone=UTC&amp;allowPublicKeyRetrieval=true&amp;createDatabaseIfNotExist=true</property>
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password">CHANGE PASS</property>

        <!-- Hibernate 6 dialect -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQLDialect</property>

        <!-- Hibernate check the mapping matches the real tables and fail if it doesn't -->
        <property name="hibernate.hbm2ddl.auto">validate</property>

        <!-- Prints generated SQL to the console - helpful while learning/debugging -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

        <!-- Register every entity class -->
        <mapping class="com.feastorder.model.Category"/>
        <mapping class="com.feastorder.model.MenuItem"/>
        <mapping class="com.feastorder.model.User"/>
        <mapping class="com.feastorder.model.Order"/>
        <mapping class="com.feastorder.model.OrderItem"/>

    </session-factory>
</hibernate-configuration>

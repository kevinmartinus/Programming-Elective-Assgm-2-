/*
    FeastOrder — Client-side JavaScript
    ------------------------------------------------------------
    TODO for your team, implement validation/interaction functions such as:

    1. validateRegisterForm()
       - Called on submit of register.jsp's form
       - Check required fields, email format, password match
       - Return false (and show inline error messages) if invalid,
         to stop the form from submitting

    2. validateLoginForm()
       - Basic non-empty checks

    3. validateCheckoutForm()
       - Ensure cart quantities are valid positive numbers before
         submitting to /placeOrder
       - Rubric 2c explicitly requires "Validate inputs using JavaScript"

    4. updateQuantity(itemId, newQty)
       - Optional: live-update subtotal display on cart page without
         a full page reload (nice touch, not required)

    5. FAQ accordion toggle (if you build one on faq.html)

    IMPORTANT: JS validation improves UX but is NOT a substitute for
    server-side validation in your Servlets — always validate on both ends.
*/

document.addEventListener('DOMContentLoaded', function () {
    // TODO: attach event listeners / form validation hooks here
});
